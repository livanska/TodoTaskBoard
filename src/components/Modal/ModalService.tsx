import React from "react";
import { styled } from "styled-components";

const Root = styled.div`
    position: absolute;
    top: 0;
`;
const ModalService = () => {
    return <Root id="modal" />;
};

export default ModalService;
